# Copy to .env and fill in. The .env file is read automatically and must never
# be committed.

# Gemini API key (https://aistudio.google.com/apikey).
GEMINI_API_KEY=your_key_here

# Optional overrides.
# DATA_ROOT=dataset
# VISION_PROVIDER=gemini
# DECISION_PROVIDER=gemini
